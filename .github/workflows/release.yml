name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version
        id: version
        run: |
          VERSION=$(cat VERSION)
          echo "version=v${VERSION}" >> $GITHUB_OUTPUT
          echo "version_name=${VERSION}" >> $GITHUB_OUTPUT

      - name: Create release package
        run: |
          # 创建带目录结构的临时目录
          mkdir -p release_temp/zentao-workflow

          # 只复制 SKILL 必须的文件到 zentao-workflow 子目录
          cp SKILL.md release_temp/zentao-workflow/
          cp -r scripts release_temp/zentao-workflow/
          cp -r assets release_temp/zentao-workflow/
          cp -r references release_temp/zentao-workflow/

          # 创建 zip 包（包含 zentao-workflow 目录）
          cd release_temp
          zip -r ../zentao-workflow-${{ steps.version.outputs.version }}.zip zentao-workflow
          cd ..

          # 清理临时目录
          rm -rf release_temp

          # 显示包内容
          echo "📦 Release package contents:"
          unzip -l zentao-workflow-${{ steps.version.outputs.version }}.zip

          # 显示包大小
          echo ""
          echo "📦 Package size:"
          ls -lh zentao-workflow-${{ steps.version.outputs.version }}.zip

      - name: Generate release notes
        id: notes
        run: |
          VERSION=${{ steps.version.outputs.version_name }}
          NOTES=$(sed -n "/## \[$VERSION\]/,/## \[/p" CHANGELOG.md | sed '$d')
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Zentao Workflow Skill ${{ steps.version.outputs.version }}
          body: |
            ## 禅道开发工作流助手 ${{ steps.version.outputs.version }}

            ${{ steps.notes.outputs.notes }}

            ---

            ## 快速安装

            **下载解压即可使用**

            1. 下载下方的 `zentao-workflow-${{ steps.version.outputs.version }}.zip`
            2. 解压到 Claude Code 技能目录：

            ```bash
            # Linux/macOS
            mkdir -p ~/.claude/skills
            unzip zentao-workflow-${{ steps.version.outputs.version }}.zip -d ~/.claude/skills/

            # Windows PowerShell
            New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude\skills"
            Expand-Archive zentao-workflow-${{ steps.version.outputs.version }}.zip -DestinationPath "$env:USERPROFILE\.claude\skills"
            ```

            3. 重启 Claude Code 即可使用

            > 💡 zip 包内已包含 `zentao-workflow/` 目录，解压后直接可用

            ## 环境要求

            只需满足以下**任一**环境：
            - Java 8+
            - Python 3.6+

            ## 包内容

            ```
            zentao-workflow/
            ├── SKILL.md              # 技能主文件
            ├── scripts/              # 内置工具（Java jar + Python）
            ├── assets/               # 模板资源
            └── references/           # 项目分析指南
            ```

          files: |
            zentao-workflow-${{ steps.version.outputs.version }}.zip
          draft: false
          prerelease: false
