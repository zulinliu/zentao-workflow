---
phase: 01-foundation-and-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/chandao_fetch/
  - scripts/chandao_fetch.py
  - scripts/worklet/
  - scripts/worklet.py
  - scripts/worklet/__init__.py
  - scripts/worklet/__main__.py
  - scripts/worklet/config.py
  - scripts/worklet/client.py
  - scripts/worklet/models.py
  - scripts/worklet/service.py
  - scripts/worklet/exporter.py
  - VERSION
autonomous: true
requirements: [FOUND-01, FOUND-12, FOUND-15]

must_haves:
  truths:
    - "The package directory is named scripts/worklet/ (not scripts/chandao_fetch/)"
    - "The wrapper script is named scripts/worklet.py (not scripts/chandao_fetch.py)"
    - "All class names use Worklet prefix instead of Chandao prefix"
    - "All internal imports use worklet package name"
    - "The __version__ string is 2.0.0"
    - "The VERSION file contains 2.0.0"
    - "python -c 'from worklet import __version__' works from scripts/ directory"
    - "grep -ri chandao scripts/ returns zero hits"
    - "grep -ri zentao scripts/ returns zero hits (excluding comments about Zentao API endpoints)"
  artifacts:
    - path: "scripts/worklet/__init__.py"
      provides: "Package init with version 2.0.0 and renamed exports"
      contains: "__version__ = \"2.0.0\""
    - path: "scripts/worklet/__main__.py"
      provides: "CLI entry point with renamed imports"
      contains: "from .config import WorkletConfig"
    - path: "scripts/worklet/config.py"
      provides: "Config class renamed to WorkletConfig"
      contains: "class WorkletConfig"
    - path: "scripts/worklet/client.py"
      provides: "API client renamed to WorkletClient"
      contains: "class WorkletClient"
    - path: "scripts/worklet/service.py"
      provides: "Service class renamed to WorkletService"
      contains: "class WorkletService"
    - path: "scripts/worklet/exporter.py"
      provides: "Markdown exporter (class name unchanged)"
      contains: "class MarkdownExporter"
    - path: "scripts/worklet/models.py"
      provides: "Data models with old Chandao prefix removed"
      contains: "class Story"
    - path: "scripts/worklet.py"
      provides: "Wrapper entry script"
      contains: "from worklet.__main__ import main"
    - path: "VERSION"
      provides: "Version file"
      contains: "2.0.0"
  key_links:
    - from: "scripts/worklet.py"
      to: "scripts/worklet/__main__.py"
      via: "from worklet.__main__ import main"
      pattern: "from worklet\\.__main__ import main"
    - from: "scripts/worklet/__main__.py"
      to: "scripts/worklet/config.py"
      via: "from .config import WorkletConfig"
      pattern: "from \\.config import WorkletConfig"
    - from: "scripts/worklet/__main__.py"
      to: "scripts/worklet/service.py"
      via: "from .service import WorkletService"
      pattern: "from \\.service import WorkletService"
    - from: "scripts/worklet/service.py"
      to: "scripts/worklet/client.py"
      via: "from .client import WorkletClient"
      pattern: "from \\.client import WorkletClient"
---

<objective>
Rename the entire Python package from chandao_fetch to worklet, updating all class names, imports, docstrings, and version numbers.

Purpose: This is the foundational rename that all other Phase 1 plans depend on. Every reference to chandao/zentao within the Python package must become worklet, and the version must be bumped to 2.0.0. After this plan, `python -c "from worklet import __version__; print(__version__)"` must print "2.0.0" when run from the scripts/ directory.

Output: A fully renamed scripts/worklet/ package with all internal consistency maintained.
</objective>

<execution_context>
@/home/liuzl/.claude/get-shit-done/workflows/execute-plan.md
@/home/liuzl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-rename/01-CONTEXT.md
@.planning/phases/01-foundation-and-rename/01-RESEARCH.md

<interfaces>
<!-- Current class names and imports that must ALL be renamed -->

From scripts/chandao_fetch/__init__.py:
```python
from .config import ChandaoConfig       # -> from .config import WorkletConfig
from .client import ChandaoClient       # -> from .client import WorkletClient
from .models import Attachment, Bug, Story, Task  # unchanged
from .exporter import MarkdownExporter  # unchanged
from .service import ChandaoService     # -> from .service import WorkletService
__version__ = "1.0.0"                   # -> "2.0.0"
```

From scripts/chandao_fetch/config.py:
```python
class ChandaoConfig:                    # -> class WorkletConfig
    WORKSPACE_CONFIG = ".chandao/config.properties"  # KEEP AS-IS in Plan 02 (Plan 03 migrates to TOML)
    GLOBAL_CONFIG = "~/.chandao/config.properties"   # KEEP AS-IS in Plan 02 (Plan 03 migrates to TOML)
```

From scripts/chandao_fetch/client.py:
```python
from .config import ChandaoConfig       # -> from .config import WorkletConfig
from .models import Attachment, Bug, Story, Task
class ChandaoClient:                    # -> class WorkletClient
    def __init__(self, config: ChandaoConfig):  # -> config: WorkletConfig
```

From scripts/chandao_fetch/service.py:
```python
from .client import ChandaoClient       # -> from .client import WorkletClient
from .config import ChandaoConfig       # -> from .config import WorkletConfig
class ChandaoService:                   # -> class WorkletService
    def __init__(self, config: ChandaoConfig):  # -> config: WorkletConfig
```

From scripts/chandao_fetch/__main__.py:
```python
from .config import ChandaoConfig       # -> from .config import WorkletConfig
from .service import ChandaoService     # -> from .service import WorkletService
```

From scripts/chandao_fetch.py (wrapper):
```python
from chandao_fetch.__main__ import main # -> from worklet.__main__ import main
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package directory and all Python source files</name>
  <files>scripts/chandao_fetch/, scripts/worklet/, scripts/worklet/__init__.py, scripts/worklet/__main__.py, scripts/worklet/config.py, scripts/worklet/client.py, scripts/worklet/models.py, scripts/worklet/service.py, scripts/worklet/exporter.py</files>
  <read_first>
    - scripts/chandao_fetch/__init__.py
    - scripts/chandao_fetch/__main__.py
    - scripts/chandao_fetch/config.py
    - scripts/chandao_fetch/client.py
    - scripts/chandao_fetch/models.py
    - scripts/chandao_fetch/service.py
    - scripts/chandao_fetch/exporter.py
  </read_first>
  <action>
Execute the rename in this exact sequence:

**Step A: Rename the package directory**
```bash
cd /home/liuzl/agent/zentao-workflow
git mv scripts/chandao_fetch scripts/worklet
```

**Step B: Rewrite scripts/worklet/__init__.py** to contain:
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Worklet - Developer workflow assistant

Fetches requirements, tasks, and bugs from Zentao and generates technical implementation plans.
"""

from .config import WorkletConfig
from .client import WorkletClient
from .models import Attachment, Bug, Story, Task
from .exporter import MarkdownExporter
from .service import WorkletService

__version__ = "2.0.0"
__all__ = [
    "WorkletConfig",
    "WorkletClient",
    "Attachment",
    "Bug",
    "Story",
    "Task",
    "MarkdownExporter",
    "WorkletService",
]
```

**Step C: Rewrite scripts/worklet/__main__.py** -- Apply these changes:
1. Module docstring: Replace "禅道数据抓取工具 - 主入口" with "Worklet - CLI entry point"
2. Replace `from .config import ChandaoConfig` with `from .config import WorkletConfig`
3. Replace `from .service import ChandaoService` with `from .service import WorkletService`
4. In `parse_args()`: Change description to "Worklet - download requirements/tasks/bugs to local Markdown files"
5. In `main()`: Replace `ChandaoConfig.load(` with `WorkletConfig.load(`
6. In `main()`: Replace `ChandaoService(config)` with `WorkletService(config)`
7. Replace `from typing import List` with `pass` (not needed in 3.10+, use `list[int]` directly)
8. Change type annotation `ids: List[int] = []` to `ids: list[int] = []`

**Step D: Rewrite scripts/worklet/config.py** -- Apply these changes:
1. Module docstring: Replace all "禅道" references with "Worklet"
2. Replace `class ChandaoConfig:` with `class WorkletConfig:`
3. Replace `"""禅道配置管理"""` with `"""Worklet configuration manager"""`
4. Keep the config paths AS-IS for now (`.chandao/config.properties`) -- Plan 03 handles TOML migration
5. In the `load()` classmethod: Change return type annotation from `"ChandaoConfig"` to `"WorkletConfig"`
6. In `_load_from_file`: Keep the `zentao.url`, `zentao.username`, `zentao.password` key parsing AS-IS -- Plan 03 handles TOML format
7. In `get_init_prompt()`: Update the prompt text to reference "worklet" instead of "chandao_fetch", but keep the configuration instructions matching current properties format (Plan 03 will update)
8. Replace `from typing import Optional, List` with just `from pathlib import Path` (use `str | None` and `list[Path]` for 3.10+ syntax per D-05)
9. Update all `Optional[str]` to `str | None`, `Optional[int]` to `int | None`, `List[Path]` to `list[Path]`
10. All Chinese comments and docstrings referencing "禅道" should be updated to use "Worklet" or English equivalents

**Step E: Rewrite scripts/worklet/client.py** -- Apply these changes:
1. Module docstring: "Worklet - API client module"
2. Replace `from .config import ChandaoConfig` with `from .config import WorkletConfig`
3. Replace `class ChandaoClient:` with `class WorkletClient:`
4. Update class docstring: "Worklet API client" (keep the safety constraint comment about read-only operations)
5. In `__init__`: Change `config: ChandaoConfig` to `config: WorkletConfig`
6. Change User-Agent header from `"ChandaoFetch-Python/1.0"` to `"Worklet/2.0"`
7. Replace `from typing import List, Optional` -- use `list[]` and `| None` directly per D-05
8. Update all type annotations: `List[Attachment]` -> `list[Attachment]`, `Optional[str]` -> `str | None`
9. Update Chinese print strings: replace "禅道" references where they appear in user-facing print messages

**Step F: Rewrite scripts/worklet/models.py** -- Apply these changes:
1. Module docstring: "Worklet - data models"
2. Replace `from typing import List, Optional` -- use built-in generics per D-05
3. Update all type annotations: `Optional[int]` -> `int | None`, `Optional[str]` -> `str | None`, `Optional[float]` -> `float | None`, `List[Attachment]` -> `list[Attachment]`, `List[str]` -> `list[str]`
4. Keep class names Story, Task, Bug, Attachment as-is (per D-03: old models just remove Chandao prefix -- these never had it)
5. Update Chinese docstrings to English: "附件实体" -> "Attachment entity", "需求实体" -> "Story entity", "任务实体" -> "Task entity", "Bug实体" -> "Bug entity"

**Step G: Rewrite scripts/worklet/service.py** -- Apply these changes:
1. Module docstring: "Worklet - service layer"
2. Replace `from .client import ChandaoClient` with `from .client import WorkletClient`
3. Replace `from .config import ChandaoConfig` with `from .config import WorkletConfig`
4. Replace `class ChandaoService:` with `class WorkletService:`
5. Update class docstring: "Worklet service" (replacing "禅道服务主类")
6. In `__init__`: Change `config: ChandaoConfig` to `config: WorkletConfig`
7. Change `self.client = ChandaoClient(config)` to `self.client = WorkletClient(config)`
8. Replace `from typing import List, Optional` -- use built-in generics per D-05
9. Update all type annotations accordingly

**Step H: Rewrite scripts/worklet/exporter.py** -- Apply these changes:
1. Module docstring: "Worklet - Markdown export module"
2. Class name `MarkdownExporter` stays the same (no Chandao prefix to remove)
3. Replace `from typing import List, Optional` -- use built-in generics per D-05
4. Update all type annotations: `List[str]` -> `list[str]`, `Optional[str]` -> `str | None`, `List[Attachment]` -> `list[Attachment]`, `Optional[List[Attachment]]` -> `list[Attachment] | None`
5. Update Chinese docstrings and comments to English

**CRITICAL: After all renames, run this comprehensive grep to verify zero chandao/zentao hits:**
```bash
grep -ri "chandao\|zentao" scripts/
```
The ONLY acceptable matches are:
- References to the Zentao API URL paths like `/story-view-`, `/task-view-`, `/bug-view-`, `/user-login.json`, `/file-download-` in client.py (these are the actual API endpoints, not naming references)
- The word "Zentao" when used as the name of the external system being accessed (e.g., "Fetches from Zentao API" is OK; "ChandaoClient" is NOT OK)

Per D-01: Full rename of package name, class names, folder names.
Per D-05: Use Python 3.10+ syntax throughout (`X | Y`, `list[T]`, `dict[K,V]`).
Per FOUND-15: __version__ = "2.0.0".
  </action>
  <verify>
    <automated>cd /home/liuzl/agent/zentao-workflow && python3 -c "import sys; sys.path.insert(0, 'scripts'); from worklet import __version__; assert __version__ == '2.0.0', f'Expected 2.0.0 got {__version__}'; print('PASS: version is 2.0.0')" && echo "Import OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/worklet/` directory exists
    - `scripts/chandao_fetch/` directory does NOT exist
    - `scripts/worklet/__init__.py` contains `__version__ = "2.0.0"`
    - `scripts/worklet/__init__.py` contains `from .config import WorkletConfig`
    - `scripts/worklet/config.py` contains `class WorkletConfig`
    - `scripts/worklet/client.py` contains `class WorkletClient`
    - `scripts/worklet/service.py` contains `class WorkletService`
    - `scripts/worklet/models.py` does NOT contain `from typing import List` or `from typing import Optional`
    - `grep -ri "ChandaoConfig\|ChandaoClient\|ChandaoService\|chandao_fetch" scripts/` returns zero hits
    - `python3 -c "import sys; sys.path.insert(0, 'scripts'); from worklet import __version__; print(__version__)"` prints "2.0.0"
  </acceptance_criteria>
  <done>Package successfully renamed from chandao_fetch to worklet with all internal imports working</done>
</task>

<task type="auto">
  <name>Task 2: Rename wrapper entry script and update VERSION file</name>
  <files>scripts/chandao_fetch.py, scripts/worklet.py, VERSION</files>
  <read_first>
    - scripts/chandao_fetch.py
    - VERSION
  </read_first>
  <action>
**Step A: Rename the wrapper script**
```bash
cd /home/liuzl/agent/zentao-workflow
git mv scripts/chandao_fetch.py scripts/worklet.py
```

**Step B: Rewrite scripts/worklet.py** to contain:
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Worklet - direct execution entry point

Usage:
    python worklet.py -t story -i 38817
    python worklet.py -t task --ids 12345,12346
    python worklet.py -t bug -i 67890 -o ~/my-output
"""

import sys
import os

# Add current directory to Python path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from worklet.__main__ import main

if __name__ == "__main__":
    main()
```

**Step C: Update VERSION file** to contain exactly:
```
2.0.0
```
(No trailing newline other than the standard single newline at end of file.)

Per FOUND-12: Entry file rename from chandao_fetch.py to worklet.py.
Per FOUND-15: VERSION file updated to 2.0.0.
  </action>
  <verify>
    <automated>cd /home/liuzl/agent/zentao-workflow && test -f scripts/worklet.py && test ! -f scripts/chandao_fetch.py && grep -q "from worklet.__main__ import main" scripts/worklet.py && grep -q "2.0.0" VERSION && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/worklet.py` exists
    - `scripts/chandao_fetch.py` does NOT exist
    - `scripts/worklet.py` contains `from worklet.__main__ import main`
    - `scripts/worklet.py` does NOT contain the string "chandao"
    - `VERSION` file contains "2.0.0"
    - `VERSION` file does NOT contain "1.6.0"
  </acceptance_criteria>
  <done>Wrapper script renamed and VERSION updated to 2.0.0, all imports point to new worklet package</done>
</task>

</tasks>

<verification>
```bash
cd /home/liuzl/agent/zentao-workflow

# 1. Package structure verification
test -d scripts/worklet/ && echo "OK: worklet dir exists" || echo "FAIL"
test ! -d scripts/chandao_fetch/ && echo "OK: old dir gone" || echo "FAIL"
test -f scripts/worklet.py && echo "OK: wrapper exists" || echo "FAIL"
test ! -f scripts/chandao_fetch.py && echo "OK: old wrapper gone" || echo "FAIL"

# 2. Import verification
python3 -c "import sys; sys.path.insert(0, 'scripts'); from worklet import __version__; assert __version__ == '2.0.0'; print('OK: import works, version 2.0.0')"

# 3. Full rename verification (the critical success criterion)
HITS=$(grep -ri "chandao" scripts/ --include="*.py" | grep -v "Zentao" | grep -v "/story-view-\|/task-view-\|/bug-view-\|/user-login\|/file-download" | wc -l)
echo "Remaining chandao references (should be 0): $HITS"

# 4. Version file
cat VERSION
```
</verification>

<success_criteria>
1. `test -d scripts/worklet/` succeeds
2. `test ! -d scripts/chandao_fetch/` succeeds
3. `python3 -c "import sys; sys.path.insert(0, 'scripts'); from worklet import __version__; print(__version__)"` prints "2.0.0"
4. `grep -ri "ChandaoConfig\|ChandaoClient\|ChandaoService\|chandao_fetch" scripts/` returns zero hits
5. `cat VERSION` prints "2.0.0"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-rename/01-02-SUMMARY.md`
</output>
