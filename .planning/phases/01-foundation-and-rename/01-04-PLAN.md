---
phase: 01-foundation-and-rename
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - pyproject.toml
  - scripts/requirements.txt
  - .gitignore
  - .release-ignore
  - SKILL.md
  - .github/workflows/release.yml
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-07, FOUND-08, FOUND-09, FOUND-11, FOUND-16]

must_haves:
  truths:
    - "pyproject.toml exists at project root with Python >= 3.10 and correct dependencies"
    - "requirements.txt has requests>=2.32.0, tomli>=2.0.0, and tomli-w>=1.0.0"
    - ".gitignore contains .worklet/ and no longer has chandao-data patterns"
    - ".release-ignore has no Java references and uses worklet naming"
    - "SKILL.md has no Java tool references and Python paths use worklet naming"
    - "release.yml packages worklet files, not chandao files, and has no Java references"
    - "README.md has no Java runtime requirement or build instructions"
    - "CLAUDE.md has no Java build/runtime/dependency sections and Python version is 3.10+"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata with setuptools backend and dependency declarations"
      contains: "requires-python = \">=3.10\""
    - path: "scripts/requirements.txt"
      provides: "Pip-installable dependency list for SKILL.md compatibility"
      contains: "requests>=2.32.0"
    - path: ".gitignore"
      provides: "Updated ignore patterns for .worklet/ output directory"
      contains: ".worklet/"
    - path: ".release-ignore"
      provides: "Updated packaging exclusions without Java references"
    - path: "SKILL.md"
      provides: "Skill manifest with Java removed and Python paths updated to worklet"
      contains: "worklet.py"
    - path: ".github/workflows/release.yml"
      provides: "CI/CD workflow with worklet naming and no Java artifacts"
      contains: "worklet"
    - path: "README.md"
      provides: "Updated README with no Java references and Python 3.10+ requirement"
    - path: "CLAUDE.md"
      provides: "Updated CLAUDE.md with no Java sections and Python 3.10+ references"
  key_links:
    - from: "pyproject.toml"
      to: "scripts/worklet/"
      via: "[tool.setuptools.packages.find] where = [\"scripts\"]"
      pattern: "where = \\[\"scripts\"\\]"
    - from: "SKILL.md"
      to: "scripts/worklet.py"
      via: "Python tool path reference"
      pattern: "worklet\\.py"
---

<objective>
Create pyproject.toml, update requirements.txt, and update all peripheral config files (including README.md and CLAUDE.md) to reflect the worklet rename and Java removal.

Purpose: This plan handles everything outside the core Python package: the project metadata (pyproject.toml per D-02), dependency declarations (requirements.txt per FOUND-11), git ignore patterns (.gitignore per FOUND-07/FOUND-08), release packaging (.release-ignore per FOUND-09), skill manifest (SKILL.md Java removal per D-08), CI/CD workflow (release.yml per D-08), and documentation files (README.md and CLAUDE.md Java removal per D-08). After this plan, the entire repository consistently uses worklet naming with no Java references.

Output: Complete project infrastructure updated for v2.0.0 Worklet identity.
</objective>

<execution_context>
@/home/liuzl/.claude/get-shit-done/workflows/execute-plan.md
@/home/liuzl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-rename/01-CONTEXT.md
@.planning/phases/01-foundation-and-rename/01-RESEARCH.md

<interfaces>
<!-- pyproject.toml structure from D-02 and Research Pattern 4 -->
```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "worklet"
version = "2.0.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["scripts"]
```

<!-- Current .gitignore patterns to replace -->
Current chandao patterns:
```
chandao-data/
chandao-data-*/
attachments/
```
Replace with:
```
.worklet/
```

<!-- Current .release-ignore Java patterns to remove -->
```
scripts/java-src/      # <- remove this line
chandao-data/          # <- replace with .worklet/
chandao-data-*/        # <- remove
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and update requirements.txt</name>
  <files>pyproject.toml, scripts/requirements.txt</files>
  <read_first>
    - scripts/requirements.txt
    - .planning/phases/01-foundation-and-rename/01-RESEARCH.md
  </read_first>
  <action>
**Part A: Create pyproject.toml at project root**

Create `/home/liuzl/agent/zentao-workflow/pyproject.toml` with this exact content:

```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "worklet"
version = "2.0.0"
description = "Developer workflow assistant - from requirement to code in one step"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "liuzl"}]
dependencies = [
    "requests>=2.32.0",
    "tomli>=2.0.0; python_version < '3.11'",
    "tomli-w>=1.0.0",
]

[tool.setuptools.packages.find]
where = ["scripts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

Per D-02: pyproject.toml at project root, setuptools backend, where = ["scripts"].
Per D-05: requires-python = ">=3.10" (FOUND-03).
Per FOUND-16: pyproject.toml manages dependencies and project metadata.
Per Research: tomli conditional on python_version < '3.11'.

**Part B: Update scripts/requirements.txt**

Rewrite `scripts/requirements.txt` to:

```
# Worklet - Python dependencies
# For SKILL.md pip install compatibility (pyproject.toml is the canonical source)

requests>=2.32.0
tomli>=2.0.0
tomli-w>=1.0.0
```

Per FOUND-11: Update to requests>=2.32.0 (security fix CVE-2024-35195), add tomli and tomli-w.
Per Claude's Discretion: Keep requirements.txt alongside pyproject.toml for SKILL.md pip install compatibility.
Note: tomli conditional (python_version < 3.11) is only in pyproject.toml; requirements.txt installs unconditionally since pip install -r doesn't support environment markers.
  </action>
  <verify>
    <automated>cd /home/liuzl/agent/zentao-workflow && test -f pyproject.toml && grep -q 'requires-python = ">=3.10"' pyproject.toml && grep -q 'where = \["scripts"\]' pyproject.toml && grep -q "requests>=2.32.0" scripts/requirements.txt && grep -q "tomli-w>=1.0.0" scripts/requirements.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at project root
    - `pyproject.toml` contains `requires-python = ">=3.10"`
    - `pyproject.toml` contains `name = "worklet"`
    - `pyproject.toml` contains `version = "2.0.0"`
    - `pyproject.toml` contains `where = ["scripts"]`
    - `pyproject.toml` contains `"requests>=2.32.0"` in dependencies
    - `pyproject.toml` contains `"tomli>=2.0.0; python_version < '3.11'"` in dependencies
    - `pyproject.toml` contains `"tomli-w>=1.0.0"` in dependencies
    - `pyproject.toml` contains `requires = ["setuptools>=68.0"]`
    - `pyproject.toml` contains `testpaths = ["tests"]`
    - `scripts/requirements.txt` contains `requests>=2.32.0`
    - `scripts/requirements.txt` contains `tomli>=2.0.0`
    - `scripts/requirements.txt` contains `tomli-w>=1.0.0`
    - `scripts/requirements.txt` does NOT contain `requests>=2.28.0`
  </acceptance_criteria>
  <done>pyproject.toml created with correct project metadata, requirements.txt updated with new dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore, .release-ignore, SKILL.md, release.yml, README.md, and CLAUDE.md</name>
  <files>.gitignore, .release-ignore, SKILL.md, .github/workflows/release.yml, README.md, CLAUDE.md</files>
  <read_first>
    - .gitignore
    - .release-ignore
    - SKILL.md
    - .github/workflows/release.yml
    - README.md
    - CLAUDE.md
  </read_first>
  <action>
**Part A: Update .gitignore**

Replace the "Project specific" section at the bottom of .gitignore. Change:
```
# Project specific
*.log
chandao-data/
chandao-data-*/
attachments/
```

To:
```
# Project specific
*.log
.worklet/
```

Keep all other sections (Python, Virtual environments, IDE, OS, Development only) unchanged.

Per FOUND-07: .worklet/ auto-added to .gitignore.
Per FOUND-08: Replace chandao-data patterns with worklet patterns.

**Part B: Update .release-ignore**

Rewrite the file to remove Java references and update naming. The full content should be:

```
# Files excluded from release packaging
# These files are not included in the release zip

# User documentation (not needed in skill context)
README.md
LICENSE
CHANGELOG.md
VERSION

# Development configuration
CLAUDE.md
CONTRIBUTING.md

# Git
.git/
.gitignore
.gitattributes

# CI/CD
.github/
.release-ignore

# IDE
.idea/
.vscode/
*.swp
*.swo

# Python caches
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.coverage

# Environment
.env
.venv/
venv/

# Temp files
*.log
*.tmp
.DS_Store
Thumbs.db

# Test and build output
.worklet/

# Project metadata (not needed in skill runtime)
pyproject.toml
```

Key changes from original:
- Removed `# Java 源码（release 只包含编译后的 JAR）` comment and `scripts/java-src/` line
- Removed `chandao-data/` and `chandao-data-*/` lines
- Removed `attachments/` line (now under .worklet/)
- Added `.worklet/` line
- Added `pyproject.toml` (not needed in skill runtime)
- Comments converted to English

Per FOUND-09: Remove Java paths and comments from .release-ignore.

**Part C: Update SKILL.md**

Make these specific edits to SKILL.md to remove Java references and update Python paths. This is a PARTIAL update for Phase 1 (Phase 5 does the full rewrite per SKILL-01 through SKILL-05).

Changes to make:
1. In the "内置工具" table: Remove the Java row entirely. Update the Python row:
   - Change `{SKILL_DIR}/scripts/chandao_fetch.py` to `{SKILL_DIR}/scripts/worklet.py`
   - Change `Python 3.6+` to `Python 3.10+`

2. In Step 1.2: Remove the Java detection line `java -version 2>&1 | head -1`

3. In Step 1.3 "选择运行时" table: Remove "仅 Java 可用" and "两者都可用" rows. Keep only "Python 可用" and "Python 不可用" scenarios.

4. In Step 1.3 "询问用户选择": Remove Java option entirely. Just check for Python.

5. In Step 3.4 "执行下载": Remove the entire "Java 版本" code block. Update the Python block:
   - Change `chandao_fetch.py` to `worklet.py`

6. In Step 3.5 "子任务检测": Remove the Java download commands. Update to use Python:
   ```bash
   python3 "{SKILL_DIR}/scripts/worklet.py" -t story -i {storyId}
   python3 "{SKILL_DIR}/scripts/worklet.py" -t task -i {parentTaskId}
   ```

7. In "注意事项": Remove "运行时优先级" line (Java > Python).

8. In "相关资源":
   - Remove `{SKILL_DIR}/scripts/chandao-fetch.jar` line
   - Change `{SKILL_DIR}/scripts/chandao_fetch/` to `{SKILL_DIR}/scripts/worklet/`

9. In the environment requirements sections: Change all "Python 3.6+" to "Python 3.10+". Remove all "Java 8+" references.

10. In Step 1.1 "检测 superpowers 技能": Keep this section as-is (unchanged).

Do NOT change:
- The `name: zentao-workflow` in frontmatter (Phase 5 handles)
- The description text or trigger keywords (Phase 5 handles)
- The overall Step structure (Phase 5 handles)
- The Step 2 config format (Plan 03 handles config format, but SKILL.md full rewrite is Phase 5)

Per D-08: Remove all Java references from SKILL.md.
Per FOUND-03: Update Python version requirement to 3.10+.

**Part D: Update .github/workflows/release.yml**

Make these specific edits:

1. In "Create release package" step:
   - Change `mkdir -p release_temp/zentao-workflow` to `mkdir -p release_temp/worklet`
   - Change `cp SKILL.md release_temp/zentao-workflow/` to `cp SKILL.md release_temp/worklet/`
   - Change all `release_temp/zentao-workflow/` to `release_temp/worklet/`
   - Remove the line: `cp scripts/chandao-fetch.jar release_temp/worklet/scripts/ 2>/dev/null || true`
   - Change `cp scripts/chandao_fetch.py` to `cp scripts/worklet.py`
   - Change `cp -r scripts/chandao_fetch` to `cp -r scripts/worklet`
   - Change zip name from `zentao-workflow-` to `worklet-`
   - Change inner directory from `zentao-workflow` to `worklet`

2. In "Create Release" step:
   - Change release name from `Zentao Workflow Skill` to `Worklet`
   - Change body header from `禅道开发工作流助手` to `Worklet`
   - Change zip filename references from `zentao-workflow-` to `worklet-`
   - Change install instructions directory from `zentao-workflow` to `worklet`
   - In environment requirements: Remove "Java 8+" line, change "Python 3.6+" to "Python 3.10+"
   - Update package contents tree to show `worklet/` instead of `zentao-workflow/`
   - Remove "scripts/  # 内置工具（Java jar + Python）" and replace with "scripts/  # Built-in tools (Python)"

Per D-08: Remove Java references from release workflow.
Per FOUND-01: Rename zentao-workflow directory to worklet in release packaging.

**Part E: Remove Java references from README.md** (per D-08)

Read README.md and make these edits:
1. Remove any "Java 8+" from runtime/dependency requirement lists or tables
2. Remove Java build instructions (e.g., `cd scripts/java-src && mvn clean package` sections)
3. Remove references to `chandao-fetch.jar` or `java-src/`
4. Update Python version requirement from "3.6+" to "3.10+" wherever it appears
5. Update any `chandao_fetch` references to `worklet` (package name, CLI usage examples)
6. Keep the overall README structure intact -- only remove/update Java-specific content

Per D-08: "清除 SKILL.md、README.md、CLAUDE.md 中所有 Java 相关引用"
Per FOUND-02: Java artifacts and all references removed.
Per FOUND-03: Python version updated to 3.10+.

**Part F: Remove Java references from CLAUDE.md** (per D-08)

Read CLAUDE.md and make these edits:
1. Remove the "Java 版本构建" section under "开发命令" (the `cd scripts/java-src && mvn clean package` block)
2. Remove Java from the "运行时依赖" table (the `Java | 8+ | 优先使用` row)
3. Remove Java from "平台要求" or similar sections listing Java as a dependency
4. Update Python version from "3.6+" to "3.10+" in all tables and text
5. Update `chandao_fetch` references to `worklet` in test command examples
6. Remove or update any references to `chandao-fetch.jar`, `java-src/`, Maven, OkHttp, Jackson, and other Java-specific dependencies from dependency tables
7. Update the "架构说明" file structure to show `scripts/worklet/` instead of `scripts/chandao_fetch/`
8. Update the "Technology Stack" section: remove Java language, Maven, OkHttp, Jackson, JUnit, Mockito, JaCoCo, and other Java-only entries
9. Keep CLAUDE.md's overall structure and non-Java content intact

Per D-08: "清除 SKILL.md、README.md、CLAUDE.md 中所有 Java 相关引用"
Per FOUND-02: Java artifacts and all references removed.
Per FOUND-03: Python version updated to 3.10+.
  </action>
  <verify>
    <automated>cd /home/liuzl/agent/zentao-workflow && ! grep -q "chandao-data" .gitignore && grep -q ".worklet/" .gitignore && ! grep -q "java-src" .release-ignore && ! grep -q "chandao-fetch.jar" SKILL.md && grep -q "worklet.py" SKILL.md && ! grep -q "chandao-fetch.jar" .github/workflows/release.yml && grep -q "worklet" .github/workflows/release.yml && ! grep -qi "java 8" README.md && ! grep -q "chandao-fetch.jar" README.md && ! grep -q "mvn clean package" CLAUDE.md && ! grep -q "chandao-fetch.jar" CLAUDE.md && grep -q "3.10+" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `.worklet/`
    - `.gitignore` does NOT contain `chandao-data`
    - `.release-ignore` does NOT contain `java-src`
    - `.release-ignore` does NOT contain `chandao-data`
    - `.release-ignore` contains `.worklet/`
    - `SKILL.md` does NOT contain `chandao-fetch.jar`
    - `SKILL.md` contains `worklet.py` (Python tool path)
    - `SKILL.md` does NOT contain `Java 版` in the tool table
    - `SKILL.md` contains `Python 3.10+` (not 3.6+)
    - `.github/workflows/release.yml` does NOT contain `chandao-fetch.jar`
    - `.github/workflows/release.yml` does NOT contain `chandao_fetch`
    - `.github/workflows/release.yml` contains `worklet.py`
    - `.github/workflows/release.yml` contains `scripts/worklet`
    - `.github/workflows/release.yml` contains `worklet-` in zip filename
    - `README.md` does NOT contain `Java 8+` as a runtime requirement
    - `README.md` does NOT contain `chandao-fetch.jar`
    - `README.md` does NOT contain `java-src`
    - `README.md` contains `3.10+` for Python version
    - `CLAUDE.md` does NOT contain `mvn clean package`
    - `CLAUDE.md` does NOT contain `chandao-fetch.jar`
    - `CLAUDE.md` does NOT contain `java-src/` (as a directory reference)
    - `CLAUDE.md` contains `3.10+` for Python version
    - `CLAUDE.md` references `scripts/worklet/` (not `scripts/chandao_fetch/`)
  </acceptance_criteria>
  <done>All peripheral files updated: .gitignore, .release-ignore, SKILL.md, release.yml, README.md, and CLAUDE.md reflect worklet naming with no Java references and Python 3.10+ requirement</done>
</task>

</tasks>

<verification>
```bash
cd /home/liuzl/agent/zentao-workflow

# 1. pyproject.toml checks
python3 -c "
import sys
if sys.version_info >= (3, 11):
    import tomllib
else:
    import tomli as tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
assert data['project']['name'] == 'worklet'
assert data['project']['version'] == '2.0.0'
assert data['project']['requires-python'] == '>=3.10'
print('pyproject.toml: OK')
"

# 2. requirements.txt checks
grep -q "requests>=2.32.0" scripts/requirements.txt && echo "requirements.txt: OK"

# 3. .gitignore checks
grep -q ".worklet/" .gitignore && ! grep -q "chandao-data" .gitignore && echo ".gitignore: OK"

# 4. SKILL.md checks
! grep -q "chandao-fetch.jar" SKILL.md && grep -q "worklet.py" SKILL.md && echo "SKILL.md: OK"

# 5. release.yml checks
! grep -q "chandao" .github/workflows/release.yml && grep -q "worklet" .github/workflows/release.yml && echo "release.yml: OK"

# 6. README.md Java removal checks
! grep -qi "java 8" README.md && ! grep -q "chandao-fetch.jar" README.md && echo "README.md: OK"

# 7. CLAUDE.md Java removal checks
! grep -q "mvn clean package" CLAUDE.md && ! grep -q "chandao-fetch.jar" CLAUDE.md && grep -q "3.10+" CLAUDE.md && echo "CLAUDE.md: OK"

# 8. No Java references anywhere (excluding .planning/ and .git/)
! grep -r "chandao-fetch.jar" . --include="*.yml" --include="*.md" --include="*.json" 2>/dev/null | grep -v ".planning/" | grep -v ".git/" && echo "No JAR refs: OK"
```
</verification>

<success_criteria>
1. `pyproject.toml` exists with correct metadata, dependencies, and package discovery
2. `scripts/requirements.txt` has updated dependencies (requests>=2.32.0, tomli, tomli-w)
3. `.gitignore` uses `.worklet/` patterns, no chandao patterns
4. `.release-ignore` has no Java references, no chandao references
5. `SKILL.md` has no Java tool references, Python paths use worklet naming
6. `release.yml` packages worklet files, no chandao/Java references
7. `README.md` has no Java runtime requirements or build instructions, Python version is 3.10+
8. `CLAUDE.md` has no Java build/runtime/dependency sections, Python version is 3.10+, uses worklet naming
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-rename/01-04-SUMMARY.md`
</output>
