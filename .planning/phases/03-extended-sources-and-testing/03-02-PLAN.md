---
phase: 03-extended-sources-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/worklet/sources/file.py
  - scripts/worklet/sources/folder.py
autonomous: true
requirements:
  - INPUT-03
  - INPUT-04

must_haves:
  truths:
    - "FileSource auto-detects file format and dispatches to correct reader"
    - "FolderSource recursively scans directory and aggregates all supported files"
  artifacts:
    - path: "scripts/worklet/sources/file.py"
      provides: "FileSource with auto-detect reader dispatch"
      exports: ["FileSource"]
    - path: "scripts/worklet/sources/folder.py"
      provides: "FolderSource with recursive scanning"
      exports: ["FolderSource"]
  key_links:
    - from: "scripts/worklet/sources/file.py"
      to: "scripts/worklet/sources/markdown.py"
      via: "MarkdownReader import"
    - from: "scripts/worklet/sources/file.py"
      to: "scripts/worklet/sources/pdf.py"
      via: "PdfReader import"
    - from: "scripts/worklet/sources/folder.py"
      to: "scripts/worklet/sources/file.py"
      via: "FileSource composition"
---

<objective>
Implement FileSource (auto-detect format and dispatch to correct reader) and FolderSource (recursive folder scanning per D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/worklet/sources/markdown.py
@scripts/worklet/sources/pdf.py
@scripts/worklet/sources/docx.py
@scripts/worklet/sources/image.py
@scripts/worklet/sources/base.py
@scripts/worklet/models.py
@.planning/phases/03-extended-sources-and-testing/03-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scripts/worklet/models.py:
```python
class BaseSource(ABC):
    @abstractmethod
    def fetch(self, identifier: str) -> Worklet: ...

@dataclass
class Worklet:
    id: str
    title: str
    content: str  # markdown
    source_type: str  # 'zentao', 'file', 'folder'
    attachments: list[Attachment] = field(default_factory=list)
    metadata: dict = field(default_factory=dict)
```

From scripts/worklet/sources/markdown.py (MarkdownSource pattern):
```python
class MarkdownSource(BaseSource):
    def __init__(self, base_path: str | Path | None = None):
        self.base_path = Path(base_path) if base_path else Path.cwd()
        self.reader = MarkdownReader()

    def fetch(self, identifier: str) -> Worklet:
        # Resolves path, reads via reader, builds Worklet
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement FileSource with reader auto-detect and dispatch</name>
  <files>scripts/worklet/sources/file.py</files>
  <read_first>
    - scripts/worklet/sources/markdown.py (MarkdownSource pattern)
    - scripts/worklet/sources/pdf.py (PdfReader)
    - scripts/worklet/sources/docx.py (DocxReader)
    - scripts/worklet/sources/image.py (ImageReader)
    - scripts/worklet/models.py (BaseSource, Worklet)
  </read_first>
  <action>
    Create scripts/worklet/sources/file.py implementing FileSource class:

    1. Import all readers: MarkdownReader, PdfReader, DocxReader, ImageReader
       (Note: These are imported at module level for SourceRegistry fallback registration)

    2. Define READER_REGISTRY list of (extension -> reader_class) tuples:
       - MarkdownReader: .md, .txt, .markdown, .mdown
       - PdfReader: .pdf
       - DocxReader: .docx
       - ImageReader: .png, .jpg, .jpeg, .gif

    3. Define _get_reader_for(path: Path) function:
       - Iterate READER_REGISTRY, return first matching reader
       - If no match: print warning per D-04 ("Warning: unsupported format {ext}, skipping {path}")
       - Return None for unsupported formats

    4. Define FileSource class inheriting from BaseSource:
       - __init__(self, base_path: str | Path | None = None)
       - fetch(self, identifier: str) -> Worklet:
         - Resolve path (absolute or relative to base_path)
         - Call _get_reader_for(path) to get appropriate reader
         - If reader is None (unsupported): raise ValueError with warning message
         - Call reader.read(path) to get RawContent
         - Extract title (first # heading or filename stem)
         - Determine source_type from extension
         - Return Worklet with content from RawContent

    5. Update pyproject.toml entry_points:
       - Add 'file = "worklet.sources.file:FileSource"'

    6. Per D-04: Unsupported formats print warning and raise ValueError
  </action>
  <verify>
    <automated>python3 -c "from worklet.sources.file import FileSource; print('FileSource imported successfully')"</automated>
  </verify>
  <done>FileSource auto-detects format, dispatches to correct reader, raises ValueError for unsupported formats with warning</done>
</task>

<task type="auto">
  <name>Task 2: Implement FolderSource with recursive scanning</name>
  <files>scripts/worklet/sources/folder.py</files>
  <read_first>
    - scripts/worklet/sources/file.py (FileSource - for composition/reader access)
    - scripts/worklet/models.py (BaseSource, Worklet)
    - .planning/phases/03-extended-sources-and-testing/03-CONTEXT.md (D-05: supported extensions)
  </read_first>
  <action>
    Create scripts/worklet/sources/folder.py implementing FolderSource class:

    1. Define SUPPORTED_EXTENSIONS per D-05: {'.md', '.txt', '.pdf', '.docx', '.png', '.jpg', '.jpeg', '.gif'}

    2. Define FolderSource class inheriting from BaseSource:
       - __init__(self, base_path: str | Path | None = None)
       - fetch(self, identifier: str) -> Worklet:
         - Validate identifier is a directory (raise ValueError if not)
         - Use pathlib.Path.rglob() with follow_symlinks=False to find all files matching SUPPORTED_EXTENSIONS
         - For each file, determine reader via FileSource._get_reader_for() or duplicate logic
         - Read content from each file using appropriate reader
         - Aggregate all content with "\n\n---\n\n" separator
         - Build Worklet with:
           - id: folder path string
           - title: folder name
           - content: aggregated content
           - source_type: 'folder'
           - metadata: {'file_count': len(files)}
         - Return Worklet

    3. Per D-04: Skip unsupported files with warning (do not raise)

    4. Update pyproject.toml entry_points:
       - Add 'folder = "worklet.sources.folder:FolderSource"'

    5. Use follow_symlinks=False in rglob to avoid infinite loops (per research pitfall 5)
  </action>
  <verify>
    <automated>python3 -c "from worklet.sources.folder import FolderSource; print('FolderSource imported successfully')"</automated>
  </verify>
  <done>FolderSource recursively scans directories, aggregates supported files, skips unsupported with warning</done>
</task>

</tasks>

<verification>
- FileSource imports without errors
- FolderSource imports without errors
- FileSource._get_reader_for dispatches to correct reader based on extension
- FolderSource.rglob finds files matching SUPPORTED_EXTENSIONS
- Unsupported formats print warning per D-04
</verification>

<success_criteria>
- `python3 -c "from worklet.sources.file import FileSource; from worklet.sources.folder import FolderSource; print('Both imported')"` succeeds
- FileSource.fetch() returns Worklet for a single file
- FolderSource.fetch() recursively scans folder and returns Worklet with aggregated content
- pyproject.toml entry_points includes 'file' and 'folder'
</success_criteria>

<output>
After completion, create `.planning/phases/03-extended-sources-and-testing/03-02-SUMMARY.md`
</output>
