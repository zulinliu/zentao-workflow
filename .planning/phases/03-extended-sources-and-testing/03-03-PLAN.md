---
phase: 03-extended-sources-and-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/__init__.py
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "pytest framework is set up with conftest.py and shared fixtures"
    - "tests directory exists and is configured in pyproject.toml"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures for all test modules"
      exports: ["temp_workspace", "mock_config", "sample_markdown"]
    - path: "tests/__init__.py"
      provides: "tests package marker"
---

<objective>
Set up pytest framework with conftest.py fixtures for all test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pyproject.toml
@scripts/worklet/config.py
@scripts/worklet/sources/markdown.py
@scripts/worklet/models.py
@.planning/phases/03-extended-sources-and-testing/03-CONTEXT.md
@.planning/phases/03-extended-sources-and-testing/03-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns from codebase. -->

From scripts/worklet/config.py:
```python
class WorkletConfig:
    def __init__(self):
        self.base_url: str | None = None
        self.username: str | None = None
        self.password: str | None = None
        self.output_dir: str = os.getcwd()
        self.connect_timeout: int = 30
        self.read_timeout: int = 60

    @classmethod
    def load(cls, workspace_dir: str | None = None, config_path: str | None = None) -> "WorkletConfig":
        ...
```

From scripts/worklet/sources/markdown.py:
```python
class MarkdownReader:
    SUPPORTED_EXTENSIONS = {'.md', '.txt', '.markdown', '.mdown'}

    @classmethod
    def can_read(cls, path: Path) -> bool:
        return path.suffix.lower() in cls.SUPPORTED_EXTENSIONS

    def read(self, path: Path) -> RawContent:
        ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests directory structure and conftest.py</name>
  <files>tests/conftest.py, tests/__init__.py</files>
  <read_first>
    - pyproject.toml (pytest configuration, testpaths)
    - scripts/worklet/config.py (WorkletConfig class)
    - scripts/worklet/sources/markdown.py (MarkdownReader pattern)
    - scripts/worklet/models.py (Worklet, RawContent)
  </read_first>
  <action>
    Create tests/conftest.py with the following fixtures per D-02 (pytest + conftest.py fixtures):

    1. temp_workspace(tmp_path) fixture:
       - Creates a temporary directory
       - Returns Path to temp directory
       - Creates .worklet subdirectory inside

    2. mock_config fixture:
       - Creates a temporary config.toml file
       - Writes minimal TOML content with test values
       - Returns Path to config file

    3. sample_markdown(temp_workspace) fixture:
       - Creates a test.md file in temp_workspace
       - Writes "# Test Title\n\nTest content." to it
       - Returns Path to the file

    4. sample_pdf(temp_workspace) fixture:
       - Creates a test.pdf placeholder file
       - Note: Real PDF testing needs markitdown installed

    5. sample_docx(temp_workspace) fixture:
       - Creates a test.docx placeholder file
       - Note: Real DOCX testing needs markitdown installed

    6. sample_image(temp_workspace) fixture:
       - Creates a test.png placeholder file (empty or minimal)
       - Returns Path to the file

    7. mock_zentao_response fixture:
       - Returns dict with mocked Zentao API response structure

    8. mock_source_registry fixture:
       - Returns SourceRegistry instance
       - Or mock registry with known sources

    Use tmp_path fixture from pytest (built-in).
    Use tempfile.TemporaryDirectory for any cleanup needs.

    Create tests/__init__.py as empty file to make tests a package.

    Verify pytest can discover tests: `pytest tests/ --collect-only`
  </action>
  <verify>
    <automated>pytest tests/ --collect-only 2>&1 | head -20</automated>
  </verify>
  <done>pytest discovers tests directory, conftest.py fixtures are available to all test modules</done>
</task>

</tasks>

<verification>
- tests/ directory created
- tests/__init__.py created (empty)
- tests/conftest.py created with all fixtures
- `pytest tests/ --collect-only` shows no errors
</verification>

<success_criteria>
- tests/conftest.py imports successfully (no syntax errors)
- `pytest tests/ --collect-only` returns without error
- Fixtures (temp_workspace, mock_config, sample_markdown) can be used in test functions
</success_criteria>

<output>
After completion, create `.planning/phases/03-extended-sources-and-testing/03-03-SUMMARY.md`
</output>
