---
phase: 03-extended-sources-and-testing
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - tests/test_config.py
  - tests/test_sources.py
  - tests/test_exporter.py
  - tests/test_readers.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "WorkletConfig loading/saving/validation is tested"
    - "ZentaoSource login/get/download with mocked API is tested"
    - "Exporter HTML-to-Markdown conversion is tested"
    - "All readers (MD/PDF/DOCX/Image) are tested"
  artifacts:
    - path: "tests/test_config.py"
      provides: "TEST-02: WorkletConfig unit tests"
    - path: "tests/test_sources.py"
      provides: "TEST-03, TEST-05: ZentaoSource and InputParser tests"
    - path: "tests/test_exporter.py"
      provides: "TEST-04: Exporter unit tests"
    - path: "tests/test_readers.py"
      provides: "TEST-06: Reader unit tests (MD/PDF/DOCX/Image)"
---

<objective>
Create comprehensive pytest unit tests covering WorkletConfig, ZentaoSource, Exporter, and all readers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/conftest.py
@scripts/worklet/config.py
@scripts/worklet/sources/zentao.py
@scripts/worklet/sources/markdown.py
@scripts/worklet/sources/pdf.py
@scripts/worklet/sources/docx.py
@scripts/worklet/sources/image.py
@scripts/worklet/exporter.py
@.planning/phases/03-extended-sources-and-testing/03-CONTEXT.md
</context>

<interfaces>
<!-- Key types and patterns from codebase. -->

From scripts/worklet/config.py:
```python
class WorkletConfig:
    WORKSPACE_CONFIG = ".worklet/config.toml"
    GLOBAL_CONFIG = "~/.worklet/config.toml"

    @classmethod
    def load(cls, workspace_dir: str | None = None, config_path: str | None = None) -> "WorkletConfig":
        ...

    def is_initialized(self) -> bool:
        return all([self.base_url, self.username, self.password])

    def save_to_workspace(self, workspace_dir: str):
        ...

    def save_to_global(self):
        ...
```

From scripts/worklet/sources/zentao.py (to be tested with mock):
```python
class ZentaoSource(BaseSource):
    def __init__(self, config: WorkletConfig):
        ...

    def fetch(self, identifier: str) -> Worklet:
        # identifier is story/task/bug ID
        ...
```

From scripts/worklet/exporter.py:
```python
class MarkdownExporter:
    def __init__(self, output_dir: str):
        ...

    def _html_to_markdown(self, html: str, attach_path: str = '') -> str:
        # Uses markdownify
        ...

    def _sanitize_filename(self, name: str | None, max_length: int = 50) -> str:
        ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test_config.py for WorkletConfig tests</name>
  <files>tests/test_config.py</files>
  <read_first>
    - tests/conftest.py
    - scripts/worklet/config.py
  </read_first>
  <action>
    Create tests/test_config.py implementing TEST-02 (WorkletConfig unit tests):

    1. Test config loading from file:
       - test_config_load_from_file: Use mock_config fixture, call WorkletConfig.load(), verify base_url/username/password are set
       - test_config_load_workspace_overrides_global: Create both workspace and global configs, verify workspace takes priority

    2. Test config validation:
       - test_is_initialized_true: When all required fields set, is_initialized() returns True
       - test_is_initialized_false: When missing fields, returns False

    3. Test config saving:
       - test_save_to_workspace: Create temp workspace, save config, verify file created with correct content
       - test_save_0600_permissions: After save, verify file permissions are 0o600 (owner read/write only)

    4. Test config source tracking:
       - test_get_config_source: After load, get_config_source() returns path to loaded file

    Use unittest.mock.patch for any filesystem operations that need mocking.
    Use the fixtures from conftest.py (temp_workspace, mock_config).
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x -v</automated>
  </verify>
  <done>WorkletConfig loading, saving, validation, and permissions tested</done>
</task>

<task type="auto">
  <name>Task 2: Create test_sources.py for ZentaoSource and InputParser tests</name>
  <files>tests/test_sources.py</files>
  <read_first>
    - tests/conftest.py
    - scripts/worklet/sources/zentao.py
    - scripts/worklet/sources/base.py
  </read_first>
  <action>
    Create tests/test_sources.py implementing TEST-03 (ZentaoSource) and TEST-05 (InputParser):

    1. Test ZentaoSource (TEST-03):
       - test_zentao_source_fetch_story: Mock Zentao API, call ZentaoSource.fetch('123'), verify returns Worklet with story data
       - test_zentao_source_fetch_task: Mock Zentao API, call ZentaoSource.fetch('task-456'), verify returns Worklet with task data
       - test_zentao_source_fetch_bug: Mock Zentao API, call ZentaoSource.fetch('bug-789'), verify returns Worklet with bug data
       - test_zentao_source_login_failure: Mock failed login, verify appropriate exception raised
       - Use unittest.mock.patch or pytest-mock to mock HTTP requests
       - Use mock_zentao_response fixture for mocked API data

    2. Test SourceRegistry (TEST-05 via INPUT-03/INPUT-04):
       - test_source_registry_get_zentao: Verify registry.get('zentao') returns ZentaoSource
       - test_source_registry_get_file: Verify registry.get('file') returns FileSource
       - test_source_registry_get_folder: Verify registry.get('folder') returns FolderSource
       - test_source_registry_list_sources: Verify all registered sources are listed

    3. Test FileSource auto-detect (TEST-05):
       - test_file_source_dispatches_markdown: Use sample_markdown fixture, verify FileSource uses MarkdownReader
       - test_file_source_dispatches_pdf: Create temp .pdf file, verify FileSource uses PdfReader
       - test_file_source_dispatches_docx: Create temp .docx file, verify FileSource uses DocxReader
       - test_file_source_dispatches_image: Create temp .png file, verify FileSource uses ImageReader
       - test_file_source_unsupported_format: Create temp .xyz file, verify ValueError with warning

    4. Test FolderSource (TEST-05 via INPUT-04):
       - test_folder_source_scans_recursively: Create nested folder structure with .md and .txt files, verify all found
       - test_folder_source_aggregates_content: Create folder with multiple files, verify aggregated content
       - test_folder_source_skips_unsupported: Create folder with .xyz file, verify warning printed but continues

    Use tempfile for creating test files.
    Use capsys or caplog to capture warning prints.
  </action>
  <verify>
    <automated>pytest tests/test_sources.py -x -v</automated>
  </verify>
  <done>ZentaoSource mocked API tests and FileSource/FolderSource dispatch tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create test_exporter.py for Exporter tests</name>
  <files>tests/test_exporter.py</files>
  <read_first>
    - tests/conftest.py
    - scripts/worklet/exporter.py
    - scripts/worklet/models.py
  </read_first>
  <action>
    Create tests/test_exporter.py implementing TEST-04 (Exporter HTML-to-MD conversion):

    1. Test _html_to_markdown conversion:
       - test_html_to_markdown_simple: Convert '<p>Hello</p>' to 'Hello'
       - test_html_to_markdown_headings: Convert '<h1>Title</h1>' to '# Title'
       - test_html_to_markdown_links: Convert '<a href="url">text</a>' to '[text](url)'
       - test_html_to_markdown_images: Convert '<img src="img.png" alt="alt"/>' to '![alt](img.png)'
       - test_html_to_markdown_lists: Convert '<ul><li>a</li><li>b</li></ul>' to '- a\n- b'
       - test_html_to_markdown_code: Convert '<code>code</code>' to '`code`'

    2. Test _sanitize_filename:
       - test_sanitize_removes_illegal_chars: Input 'file:name?*' should return 'file_name_'
       - test_sanitize_truncates_long_names: Input very long name (>50 chars), verify hash suffix added
       - test_sanitize_empty_name: Input None or empty, verify returns 'unnamed'

    3. Test img tag with attach_path:
       - test_html_to_markdown_img_with_attach_path: Verify img src is prepended with attach_path

    4. Test _process_content:
       - test_process_content_normalizes_newlines: Multiple blank lines normalized to two

    5. Test _append_attachments:
       - test_append_attachments_image: Attachment with is_image()=True generates markdown image syntax
       - test_append_attachments_file: Non-image attachment generates link syntax

    Use the MarkdownExporter instantiated with temp output_dir.
    No need to test export_story/export_task/export_bug (those call _ methods).
  </action>
  <verify>
    <automated>pytest tests/test_exporter.py -x -v</automated>
  </verify>
  <done>Exporter HTML-to-Markdown conversion and edge cases tested</done>
</task>

<task type="auto">
  <name>Task 4: Create test_readers.py for all Reader tests</name>
  <files>tests/test_readers.py</files>
  <read_first>
    - tests/conftest.py
    - scripts/worklet/sources/markdown.py
    - scripts/worklet/sources/pdf.py
    - scripts/worklet/sources/docx.py
    - scripts/worklet/sources/image.py
  </read_first>
  <action>
    Create tests/test_readers.py implementing TEST-06 (Reader unit tests):

    1. Test MarkdownReader:
       - test_markdown_can_read_md: Verify can_read returns True for .md
       - test_markdown_can_read_txt: Verify can_read returns True for .txt
       - test_markdown_cannot_read_pdf: Verify can_read returns False for .pdf
       - test_markdown_read_file: Use sample_markdown fixture, verify read() returns RawContent with correct content
       - test_markdown_read_nonexistent: Verify FileNotFoundError raised for missing file

    2. Test PdfReader (if markitdown available, skip otherwise):
       - test_pdf_can_read: Verify can_read returns True for .pdf
       - test_pdf_cannot_read_md: Verify can_read returns False for .md
       - test_pdf_read_file: If markitdown installed, test with real PDF; else skip with pytest.skip
       - test_pdf_read_nonexistent: Verify FileNotFoundError raised

    3. Test DocxReader (if markitdown available, skip otherwise):
       - test_docx_can_read: Verify can_read returns True for .docx
       - test_docx_cannot_read_md: Verify can_read returns False for .md
       - test_docx_read_file: If markitdown installed, test with real DOCX; else skip
       - test_docx_read_nonexistent: Verify FileNotFoundError raised

    4. Test ImageReader:
       - test_image_can_read_png: Verify can_read returns True for .png
       - test_image_can_read_jpg: Verify can_read returns True for .jpg/.jpeg
       - test_image_can_read_gif: Verify can_read returns True for .gif
       - test_image_cannot_read_md: Verify can_read returns False for .md
       - test_image_read_returns_markdown_reference: Verify read() returns RawContent with format='markdown' and content containing image reference syntax
       - test_image_read_nonexistent: Verify FileNotFoundError raised

    5. Test unsupported format behavior:
       - test_unsupported_format_raises_error: For any reader, unsupported extension raises ValueError

    Use pytest.mark.skipif to skip tests when markitdown not installed.
    Use fixtures from conftest.py (sample_markdown, sample_pdf, sample_docx, sample_image).
  </action>
  <verify>
    <automated>pytest tests/test_readers.py -x -v</automated>
  </verify>
  <done>All readers (Markdown, Pdf, Docx, Image) tested with correct behavior</done>
</task>

</tasks>

<verification>
- All four test files created
- `pytest tests/test_config.py -x -v` passes
- `pytest tests/test_sources.py -x -v` passes
- `pytest tests/test_exporter.py -x -v` passes
- `pytest tests/test_readers.py -x -v` passes
</verification>

<success_criteria>
- `pytest tests/ --tb=short` runs all tests with 0 failures (or expected skips)
- Each test file covers the specified requirements
- Tests use fixtures from conftest.py where applicable
- Tests mock external dependencies (Zentao API) appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/03-extended-sources-and-testing/03-04-SUMMARY.md`
</output>
