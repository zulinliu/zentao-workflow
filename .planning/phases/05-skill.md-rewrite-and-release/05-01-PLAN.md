---
phase: 05-skill-md-rewrite-and-release
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - CLAUDE.md
  - CONTRIBUTING.md
  - assets/tech_plan_template.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - DOC-01
  - DOC-02
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "SKILL.md triggers on generic dev keywords (not zentao/chandao)"
    - "SKILL.md uses ${CLAUDE_SKILL_DIR} for all path references"
    - "SKILL.md description is under 250 characters"
    - "SKILL.md asks user for input source (Zentao API or local file)"
    - "CLAUDE.md reflects Worklet name and v2.0.0 architecture"
    - "CONTRIBUTING.md references worklet not zentao-workflow"
    - "java_project_guide.md is removed"
  artifacts:
    - path: "SKILL.md"
      provides: "Rewritten skill manifest with Worklet branding"
      contains: "worklet"
    - path: "CLAUDE.md"
      provides: "Developer guide updated to v2.0.0"
      contains: "2.0.0"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guide with worklet references"
    - path: "assets/tech_plan_template.md"
      provides: "Source-agnostic template"
  key_links:
    - from: "SKILL.md"
      to: "${CLAUDE_SKILL_DIR}/scripts/worklet.py"
      via: "python3 command"
---

<objective>
Rewrite SKILL.md with Worklet branding and update all documentation files to v2.0.0. This is Wave 1 - the core skill rewrite and documentation updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-skill.md-rewrite-and-release/05-CONTEXT.md
@.planning/phases/05-skill.md-rewrite-and-release/05-RESEARCH.md

# User Decisions (locked - must implement exactly)
- D-01: Trigger words: 开发需求、优化功能、修复bug、重构、开发、优化 (remove zentao/chandao)
- D-02: Description single-line <250 chars, English-first imperative tone
- D-03: All paths use ${CLAUDE_SKILL_DIR}
- D-04: Entry asks user source (Zentao API or local file)
- D-05: VERSION and docs sync to 2.0.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md with Worklet branding and generic triggers</name>
  <files>SKILL.md</files>
  <action>
    Complete rewrite of SKILL.md with:

    **YAML frontmatter:**
    - name: worklet
    - description: Single line under 250 chars, English imperative tone. Example: "Worklet - 从需求到代码一键启动的开发者工作流助手" (per D-02)
    - Remove ALL zentao/chandao specific trigger words

    **Trigger keywords (per D-01):**
    - 开发需求、优化功能、修复bug、重构、开发、优化
    - Remove: 禅道、zentao、chandao、Bug (keep bug in lowercase)

    **Path references (per D-03):**
    - Replace all {SKILL_DIR} with ${CLAUDE_SKILL_DIR}
    - Example: python3 ${CLAUDE_SKILL_DIR}/scripts/worklet.py

    **New Step 2.5 for source selection (per D-04):**
    After environment detection, ask user:
    ```
    AskUserQuestion:
      questions:
        - question: "需求从哪里获取？"
          header: "需求来源"
          options:
            - label: "禅道 API"
              description: "从禅道服务器下载需求/任务/Bug"
            - label: "本地文件"
              description: "从本地文件/文件夹读取"
    ```

    **Updated content structure:**
    - Step 1: Environment detection (Python only, remove Java references)
    - Step 2: Config initialization (.worklet/config.toml)
    - Step 2.5: Source selection prompt (NEW)
    - Step 3: Download content (ZentaoSource or FileSource based on selection)
    - Step 4: Technical plan generation
    - Step 5: Output plan
    - Step 6: Begin coding

    **Remove from old SKILL.md:**
    - All Java references and Java version table
    - zentao-workflow references
    - .chandao/config.properties references (now .worklet/config.toml)
    - Java_project_guide.md reference

    **Keep in references section:**
    - react_project_guide.md (still relevant)
    - tech_plan_template.md
  </action>
  <verify>
    <automated>
      # Verify trigger words removed
      ! grep -qi "zentao\|chandao\|禅道" SKILL.md && \
      # Verify path format
      grep -q '\${CLAUDE_SKILL_DIR}' SKILL.md && \
      # Verify no {SKILL_DIR} remains
      ! grep -q '{SKILL_DIR}' SKILL.md && \
      # Verify source prompt exists
      grep -q "需求从哪里获取" SKILL.md && \
      # Verify description length < 250
      [ $(sed -n '/^description: |$/,/^---$/p' SKILL.md | head -1 | wc -c) -lt 250 ]
    </automated>
  </verify>
  <done>
    SKILL.md rewritten with Worklet branding, generic triggers, ${CLAUDE_SKILL_DIR} paths, source selection prompt, and version 2.0.0
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md to Worklet v2.0.0</name>
  <files>CLAUDE.md</files>
  <action>
    Complete update of CLAUDE.md:

    **Version header:**
    - Version: 2.0.0 (not 1.6.0)

    **Project description:**
    - Update to reflect Worklet name and v2.0.0 multi-source architecture
    - Remove all references to zentao-workflow
    - Add .worklet/config.toml references (not .chandao/config.properties)

    **SKILL.md version in docs:**
    - Update "SKILL.md v1.5.0" references to "SKILL.md v2.0.0"

    **Python module architecture section:**
    - Keep current structure (already has worklet references)

    **Security constraint update:**
    - Change "ChandaoClient" to "WorkletClient" or appropriate name

    **Configuration section:**
    - Change "~/.chandao/config.properties" to "~/.worklet/config.toml"
    - Change format from Properties to TOML

    **Project section (GSD block):**
    - Ensure Project name is "Worklet - 开发工作流助手"
    - Core Value statement already correct

    **Remove:**
    - All zentao/chandao legacy references
  </action>
  <verify>
    <automated>
      # Verify version updated
      grep -q "版本.*2.0.0\|Version.*2.0.0" CLAUDE.md && \
      # Verify no chandao references
      ! grep -qi "chandao\|zentao" CLAUDE.md | grep -v "zentao-workflow" && \
      # Verify .worklet/config.toml reference exists
      grep -q "\.worklet/config\.toml" CLAUDE.md
    </automated>
  </verify>
  <done>
    CLAUDE.md updated to v2.0.0 with Worklet branding, .worklet/config.toml references, and removed zentao/chandao legacy content
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CONTRIBUTING.md and reference docs</name>
  <files>CONTRIBUTING.md, assets/tech_plan_template.md, references/java_project_guide.md</files>
  <action>
    **CONTRIBUTING.md updates:**
    - Rename project references: zentao-workflow -> worklet
    - Remove Java build instructions (no longer applicable)
    - Update file management rules: .chandao -> .worklet
    - Update release package name: zentao-workflow-vX.X.X.zip -> worklet-vX.X.X.zip
    - Update SKILL must-have files table: remove Java references
    - Update branch name if applicable

    **assets/tech_plan_template.md updates:**
    - Remove "禅道链接" field from header (source-agnostic now)
    - Change "禅道需求" in appendix to "需求文档"
    - Keep generic structure (already mostly source-agnostic)

    **references/java_project_guide.md:**
    - Per DOC-05: This file should be removed since Java is no longer supported
    - Delete this file: rm references/java_project_guide.md
  </action>
  <verify>
    <automated>
      # CONTRIBUTING.md: no zentao-workflow
      ! grep -qi "zentao-workflow" CONTRIBUTING.md && \
      # tech_plan_template.md: no "禅道链接"
      ! grep -q "禅道链接" assets/tech_plan_template.md && \
      # java_project_guide.md should not exist
      ! [ -f references/java_project_guide.md ]
    </automated>
  </verify>
  <done>
    CONTRIBUTING.md updated to worklet, tech_plan_template.md made source-agnostic, java_project_guide.md removed
  </done>
</task>

</tasks>

<verification>
# Phase 5 Wave 1 verification commands
echo "=== SKILL.md verification ===" && \
grep -c '\${CLAUDE_SKILL_DIR}' SKILL.md && \
! grep -qi "zentao\|chandao" SKILL.md && \
echo "=== CLAUDE.md verification ===" && \
grep -q "2.0.0" CLAUDE.md && \
! grep -qi "zentao-workflow" CLAUDE.md && \
echo "=== CONTRIBUTING.md verification ===" && \
! grep -qi "zentao-workflow" CONTRIBUTING.md && \
echo "=== java_project_guide.md removed ===" && \
! [ -f references/java_project_guide.md ]
</verification>

<success_criteria>
1. SKILL.md triggers on generic dev keywords (开发需求/优化功能/修复bug/重构/开发/优化)
2. SKILL.md uses ${CLAUDE_SKILL_DIR} for all paths
3. SKILL.md asks user for input source (Zentao API or local file)
4. SKILL.md description < 250 characters
5. CLAUDE.md version shows 2.0.0
6. CONTRIBUTING.md references worklet not zentao-workflow
7. java_project_guide.md removed
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill.md-rewrite-and-release/05-01-SUMMARY.md`
</output>
