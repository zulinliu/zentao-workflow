---
phase: 05-skill-md-rewrite-and-release
plan: 05-02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - README.md
  - VERSION
  - CHANGELOG.md
  - .github/workflows/release.yml
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04

must_haves:
  truths:
    - "VERSION contains exactly 2.0.0"
    - "CHANGELOG.md has complete v2.0.0 section with today's date"
    - "README.md shows Worklet v2.0.0 with correct install instructions"
    - "release.yml packages worklet-*.zip with correct structure"
    - "No Java references in release artifacts"
  artifacts:
    - path: "VERSION"
      provides: "Version file with 2.0.0"
    - path: "CHANGELOG.md"
      provides: "Changelog with v2.0.0 entry"
      contains: "## [2.0.0]"
    - path: "README.md"
      provides: "README updated to Worklet v2.0.0"
    - path: ".github/workflows/release.yml"
      provides: "CI/CD configured for worklet package"
  key_links:
    - from: "release.yml"
      to: "worklet-${{ version }}.zip"
      via: "package creation"
---

<objective>
Update README.md, VERSION, CHANGELOG.md, and verify release.yml for v2.0.0 release. This is Wave 2 - documentation finalization and CI/CD verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-skill.md-rewrite-and-release/05-CONTEXT.md
@.planning/phases/05-skill.md-rewrite-and-release/05-RESEARCH.md

# From Wave 1 SUMMARY (read after 05-01 executes):
@.planning/phases/05-skill.md-rewrite-and-release/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update VERSION to 2.0.0 and verify CHANGELOG.md</name>
  <files>VERSION, CHANGELOG.md</files>
  <action>
    **VERSION file:**
    - Verify content is exactly: 2.0.0
    - If not, update: echo "2.0.0" > VERSION

    **CHANGELOG.md updates:**
    - Create v2.0.0 section under [Unreleased]
    - Move entries from [Unreleased] to [2.0.0] section with today's date
    - Add comprehensive v2.0.0 changelog entry including:
      - rename from zentao-workflow to worklet
      - Java runtime removed, Python only
      - Multi-source support (Zentao API + local files)
      - markdownify for HTML-to-Markdown conversion
      - BaseSource ABC with ZentaoSource and FileSource
      - InputParser auto-detection
      - Environment caching with 24h TTL
      - superpowers npx detection upgrade
      - SKILL.md trigger keywords updated to generic dev terms

    **CHANGELOG structure should be:**
    ```markdown
    ## [Unreleased]

    ## [2.0.0] - 2026-04-05

    ### Added
    - Multi-source support: Zentao API and local file/folder input
    - BaseSource ABC with ZentaoSource and FileSource implementations
    - InputParser for automatic input type detection
    - Environment caching with 24h TTL
    - superpowers npx-based detection

    ### Changed
    - Renamed project from zentao-workflow to worklet
    - SKILL.md triggers updated to generic dev keywords
    - All path references use ${CLAUDE_SKILL_DIR}
    - HTML-to-Markdown conversion using markdownify library

    ### Removed
    - Java runtime and chandao-fetch.jar
    - Java project analysis guide
    - All zentao/chandao specific naming

    ### Fixed
    - [list any fixed issues]
    ```
  </action>
  <verify>
    <automated>
      # VERSION check
      grep -q "^2.0.0$" VERSION && \
      # CHANGELOG has v2.0.0 section
      grep -q "## \[2.0.0\]" CHANGELOG.md && \
      # CHANGELOG has today's date
      grep -q "2026-04-05" CHANGELOG.md
    </automated>
  </verify>
  <done>
    VERSION set to 2.0.0, CHANGELOG.md has complete v2.0.0 entry with all major changes documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for Worklet v2.0.0</name>
  <files>README.md</files>
  <action>
    Complete rewrite of README.md:

    **Header section:**
    - Title: "Worklet - 开发工作流助手" (not "Zentao Workflow Skill")
    - Version badge: 2.0.0
    - License badge
    - Claude Code Skill badge
    - GitHub release badge pointing to worklet repo

    **Description:**
    - "Worklet (Workflow + Applet) 是一个 Claude Code Skill，为开发者提供轻量级的开发工作流自动化。它从多种来源（禅道 API、本地文件/文件夹）获取需求/任务/Bug 文档，结合项目代码自动生成技术实现方案，并驱动编码执行。"

    **Installation section:**
    - From Releases: worklet-v2.0.0.zip (not zentao-workflow)
    - From GitHub: clone zulinliu/worklet.git (not zentao-workflow)
    - Update all paths to use worklet

    **Environment requirements:**
    - Python 3.10+ only (remove Java mention)
    - superpowers plugin 5.0.6+

    **Features section:**
    - Multi-source input (Zentao API + local files)
    - Intelligent auto-detection
    - Cross-platform support
    - Interactive configuration
    - Complete attachment download

    **Trigger keywords (updated):**
    - 开发需求、优化功能、修复bug、重构、开发、优化
    - Remove: 禅道、zentao、chandao specific triggers

    **Technical implementation section:**
    - Remove v1.5.0 comparison table (old)
    - Add v2.0.0 features overview

    **Output file structure:**
    - Update to show .worklet/ directory (not .chandao/)
    - Update example paths

    **Version history table:**
    - Add v2.0.0 entry
    - Update all links to GitHub

    **Badges and links:**
    - Update all GitHub URLs to zulinliu/worklet
  </action>
  <verify>
    <automated>
      # README.md has correct title
      grep -q "Worklet - 开发工作流助手" README.md && \
      # Version 2.0.0
      grep -q "2.0.0" README.md && \
      # No zentao-workflow references
      ! grep -qi "zentao-workflow" README.md && \
      # No .chandao references
      ! grep -q "\.chandao" README.md && \
      # Has new trigger keywords
      grep -q "开发需求\|优化功能\|修复bug\|重构" README.md
    </automated>
  </verify>
  <done>
    README.md rewritten for Worklet v2.0.0 with correct branding, install instructions, features, and version history
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify release.yml is correct for Worklet packaging</name>
  <files>.github/workflows/release.yml</files>
  <action>
    **Verify release.yml is correct (read-only check):**

    The release.yml should already be updated for worklet (per 05-RESEARCH.md). Verify:

    1. Package name uses worklet-${{ version }}.zip (not zentao-workflow)
    2. Package contains worklet/ directory structure
    3. Release name is "Worklet ${{ version }}"
    4. No Java files are included
    5. scripts/worklet.py is included
    6. scripts/worklet/ package is included

    **If any issues found, fix:**
    - Ensure zip structure: worklet/ subdirectory inside zip
    - Ensure release notes reference worklet
    - Ensure package name is worklet-*.zip

    **Verification commands to run:**
    ```bash
    # Check zip structure in release.yml
    grep -A5 "mkdir -p release_temp" .github/workflows/release.yml

    # Check package name
    grep "worklet-" .github/workflows/release.yml

    # Check no Java references
    ! grep -qi "java" .github/workflows/release.yml
    ```
  </action>
  <verify>
    <automated>
      # release.yml uses correct package name
      grep -q "worklet-\${{" .github/workflows/release.yml && \
      # release.yml creates worklet subdirectory
      grep -q "mkdir -p release_temp/worklet" .github/workflows/release.yml && \
      # No Java references
      ! grep -qi "java" .github/workflows/release.yml && \
      # Release name uses Worklet
      grep -q "Worklet \${{" .github/workflows/release.yml
    </automated>
  </verify>
  <done>
    release.yml verified correct for Worklet v2.0.0 packaging with no Java files
  </done>
</task>

</tasks>

<verification>
# Phase 5 Wave 2 verification commands
echo "=== VERSION ===" && cat VERSION && \
echo "=== CHANGELOG v2.0.0 section ===" && grep -A2 "## \[2.0.0\]" CHANGELOG.md && \
echo "=== README.md title ===" && head -1 README.md && \
echo "=== release.yml package name ===" && grep "worklet-" .github/workflows/release.yml | head -2
</verification>

<success_criteria>
1. VERSION contains "2.0.0"
2. CHANGELOG.md has complete v2.0.0 section with today's date
3. README.md shows Worklet v2.0.0 with correct install instructions
4. release.yml packages worklet-*.zip with correct structure
5. No Java references in any release artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill.md-rewrite-and-release/05-02-SUMMARY.md`
</output>
