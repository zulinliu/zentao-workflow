---
phase: 05-skill-md-rewrite-and-release
plan: 05-03
type: checkpoint:human-action
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - REL-05

user_setup:
  - service: github
    why: "GitHub repository rename is a user action that cannot be automated"
    action_required: "Rename GitHub repository from 'zentao-workflow' to 'worklet'"
    dashboard_url: "https://github.com/zulinliu/zentao-workflow/settings"

must_haves:
  truths:
    - "GitHub repository renamed from zentao-workflow to worklet"
    - "Local git remote URL updated to zulinliu/worklet"
    - "All Phase 5 success criteria verified"
  artifacts:
    - path: "GitHub repo"
      provides: "Repository renamed to worklet"
    - path: ".git/config"
      provides: "Remote URL updated to worklet"
  key_links:
    - from: "local repo"
      to: "github.com/zulinliu/worklet"
      via: "git remote"
---

<objective>
Execute GitHub repository rename from zentao-workflow to worklet and perform final Phase 5 verification. This is Wave 3 - the final user action and verification step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-skill.md-rewrite-and-release/05-CONTEXT.md
@.planning/phases/05-skill.md-rewrite-and-release/05-RESEARCH.md
@.planning/phases/05-skill.md-rewrite-and-release/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: GitHub repository rename (user action required)</name>
  <files>N/A - GitHub operation</files>
  <action>
    **IMPORTANT: This is a user action that CANNOT be automated.**

    User must manually rename the GitHub repository:

    1. Go to: https://github.com/zulinliu/zentao-workflow/settings
    2. Under "Repository name" field, change "zentao-workflow" to "worklet"
    3. Click "Rename"
    4. Wait for redirect to new URL

    **After rename, verify:**
    - Repository URL changes from github.com/zulinliu/zentao-workflow to github.com/zulinliu/worklet
    - All existing URLs (issues, PRs, releases) will redirect automatically
    - Git remote needs update: `git remote set-url origin https://github.com/zulinliu/worklet.git`

    **Local repo update (after user confirms rename):**
    ```bash
    git remote set-url origin https://github.com/zulinliu/worklet.git
    git fetch origin
    ```
  </action>
  <verify>
    <automated>
      # After user confirms, verify remote URL updated
      git remote get-url origin | grep -q "zulinliu/worklet"
    </automated>
  </verify>
  <done>
    GitHub repository renamed to worklet and local remote URL updated
  </done>
</task>

<task type="auto">
  <name>Task 2: Final Phase 5 verification</name>
  <files>SKILL.md, CLAUDE.md, README.md, VERSION, CHANGELOG.md</files>
  <action>
    **Final verification checklist for Phase 5 completion:**

    1. **SKILL.md verification:**
       - [ ] Triggers on generic dev keywords (grep for "开发需求\|优化功能\|修复bug")
       - [ ] Uses ${CLAUDE_SKILL_DIR} (grep count > 0)
       - [ ] No {SKILL_DIR} remains (grep returns empty)
       - [ ] Has source selection prompt (grep for "需求从哪里获取")
       - [ ] Description length < 250 chars
       - [ ] No zentao/chandao references

    2. **Documentation verification:**
       - [ ] CLAUDE.md version is 2.0.0
       - [ ] README.md shows Worklet v2.0.0
       - [ ] CONTRIBUTING.md references worklet (not zentao-workflow)
       - [ ] java_project_guide.md removed

    3. **Version verification:**
       - [ ] VERSION = 2.0.0
       - [ ] CHANGELOG.md has v2.0.0 section with date
       - [ ] All docs reference 2.0.0

    4. **Release verification:**
       - [ ] release.yml creates worklet-*.zip (not zentao-workflow-*.zip)
       - [ ] release.yml has no Java references
       - [ ] Package structure correct

    **Run comprehensive verification:**
    ```bash
    echo "=== Phase 5 Final Verification ==="
    echo ""
    echo "1. SKILL.md checks:"
    echo "   - Generic triggers: $(grep -c '开发需求\|优化功能\|修复bug\|重构' SKILL.md) found"
    echo "   - \${CLAUDE_SKILL_DIR}: $(grep -c '\${CLAUDE_SKILL_DIR}' SKILL.md) occurrences"
    echo "   - No {SKILL_DIR}: $(grep -c '{SKILL_DIR}' SKILL.md || echo 0) found"
    echo "   - Source prompt: $(grep -c '需求从哪里获取' SKILL.md) found"
    echo ""
    echo "2. Version checks:"
    echo "   - VERSION: $(cat VERSION)"
    echo "   - CHANGELOG v2.0.0: $(grep -c '## \[2.0.0\]' CHANGELOG.md) found"
    echo ""
    echo "3. README checks:"
    echo "   - Title: $(head -1 README.md)"
    echo "   - Version 2.0.0: $(grep -c '2.0.0' README.md) found"
    echo ""
    echo "4. GitHub repo:"
    echo "   - Remote URL: $(git remote get-url origin)"
    ```
  </action>
  <verify>
    <automated>
      # SKILL.md core checks
      grep -q '开发需求\|优化功能\|修复bug' SKILL.md && \
      grep -q '\${CLAUDE_SKILL_DIR}' SKILL.md && \
      ! grep -q '{SKILL_DIR}' SKILL.md && \
      grep -q '需求从哪里获取' SKILL.md && \
      ! grep -qi 'zentao\|chandao' SKILL.md && \
      # Version files
      grep -q "^2.0.0$" VERSION && \
      grep -q "## \[2.0.0\]" CHANGELOG.md && \
      # README
      grep -q "Worklet" README.md && \
      grep -q "2.0.0" README.md && \
      # GitHub remote
      git remote get-url origin | grep -q "worklet"
    </automated>
  </verify>
  <done>
    All Phase 5 success criteria verified:
    - SKILL.md has generic triggers and ${CLAUDE_SKILL_DIR} paths
    - All docs updated to v2.0.0 with Worklet branding
    - release.yml configured for worklet package
    - GitHub repo renamed to worklet
  </done>
</task>

</tasks>

<verification>
# Comprehensive Phase 5 verification
echo "=== Phase 5 Complete Verification ==="
echo ""
echo "SKILL.md:"
echo "  - Generic triggers: $(grep -E '开发需求|优化功能|修复bug|重构' SKILL.md | wc -l) trigger phrases found"
echo "  - \${CLAUDE_SKILL_DIR}: $(grep -c '\${CLAUDE_SKILL_DIR}' SKILL.md) occurrences"
echo "  - Source prompt: $(grep -c '需求从哪里获取' SKILL.md)"
echo ""
echo "Documents:"
echo "  - VERSION: $(cat VERSION)"
echo "  - CHANGELOG v2.0.0: $(grep -c '## \[2.0.0\]' CHANGELOG.md)"
echo "  - README Worklet: $(grep -c 'Worklet' README.md)"
echo ""
echo "GitHub:"
echo "  - Remote: $(git remote get-url origin)"
</verification>

<success_criteria>
1. SKILL.md triggers on generic dev keywords (not zentao/chandao)
2. SKILL.md uses ${CLAUDE_SKILL_DIR} for all paths
3. SKILL.md asks user for input source (Zentao API or local file)
4. SKILL.md description < 250 characters
5. CLAUDE.md, CONTRIBUTING.md, README.md all reflect Worklet v2.0.0
6. VERSION is 2.0.0
7. CHANGELOG.md has complete v2.0.0 section
8. release.yml produces worklet-*.zip package
9. GitHub repository renamed to "worklet"
10. All Phase 5 requirements verified (SKILL-01~05, DOC-01~02, DOC-04~05, REL-01~05)
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill.md-rewrite-and-release/05-03-SUMMARY.md`
</output>
